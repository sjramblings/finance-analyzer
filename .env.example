# Server Configuration
PORT=3000

# ==============================================
# AI Provider Configuration
# ==============================================
# Choose ONE of the following options:
#
# Option 1: Direct Anthropic API
# Use this if you have an Anthropic API key
ANTHROPIC_API_KEY=your_api_key_here
#
# Option 2: AWS Bedrock with Named Profile (RECOMMENDED)
# Use this to reference a profile from ~/.aws/credentials
# AWS_REGION=us-west-2
# AWS_PROFILE=my-profile-name
#
# Example ~/.aws/credentials file:
# [my-profile-name]
# aws_access_key_id = AKIA...
# aws_secret_access_key = ...
#
# Option 3: AWS Bedrock with Explicit Credentials
# Use this to provide credentials directly
# AWS_REGION=us-west-2
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_SESSION_TOKEN=your_session_token (optional, for temporary credentials)
#
# Option 4: AWS Bedrock with Default Credentials
# Set only AWS_REGION to use default AWS credential providers:
# - ~/.aws/credentials file (default profile)
# - EC2 instance profile
# - ECS task role
# - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# AWS_REGION=us-west-2
#
# Note: System will use AWS Bedrock if AWS_REGION is set,
# otherwise falls back to ANTHROPIC_API_KEY
# Credential priority: AWS_PROFILE > Explicit credentials > Default providers
# ==============================================

# Database
DB_PATH=./data/transactions.db

# Upload Configuration
UPLOAD_DIR=./data/uploads
MAX_UPLOAD_SIZE=10485760

# Reports
REPORTS_DIR=./data/reports

# CORS
CORS_ORIGIN=http://localhost:5173
